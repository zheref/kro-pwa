{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "worktree": { "baseRef": "origin/main" },
  "permissions": {
    "deny": ["Bash(git push --no-verify*)", "Bash(rm -rf /)", "Bash(rm -rf /*)"]
  }
}
