'use client'

import { SessionDestinationPage } from '@kro/app'

/** Client wrapper (`RC-39`): imports the Page, forwards props, ≤10 lines. */
export function ExecutePageClient() {
  return <SessionDestinationPage />
}
